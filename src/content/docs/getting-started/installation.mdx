---
title: Installation
description: How to install and set up Ferret
sidebar:
  order: 2
banner:
  content: |
    Compiler surface is still evolving. Expect breaking changes.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Install (recommended)

<Tabs>
  <TabItem label="Linux | Mac">
    ```bash
    curl -fsSL https://raw.githubusercontent.com/Ferret-Language/Ferret/refs/heads/main/installers/install.sh | bash
    ```
  </TabItem>
  <TabItem label="Windows (PowerShell)">
    ```powershell
    Invoke-WebRequest https://raw.githubusercontent.com/Ferret-Language/Ferret/refs/heads/main/installers/install.ps1 -OutFile ferret.ps1
    powershell -NoProfile -ExecutionPolicy Bypass -File .\ferret.ps1
    ```
  </TabItem>
  <TabItem label="Windows (CMD)">
    ```bat
    curl -fsSLO https://raw.githubusercontent.com/Ferret-Language/Ferret/refs/heads/main/installers/install.cmd
    ferret.cmd
    ```
  </TabItem>
</Tabs>

:::note
Default install locations are `~/.local/ferret` on Linux/macOS and `%LOCALAPPDATA%\Ferret` on Windows.
:::

## Verify installation

```bash
ferret --help
```

## Build from source

You need Go in `PATH`.

```bash
git clone https://github.com/Ferret-Language/Ferret.git
cd Ferret/compiler
./build.sh
```

Windows:

```bat
cd compiler
build.bat
```

Build artifacts are written under `build/core` and `build/toolchain`.

## Running Ferret programs

```bash
ferret run main.fer
```
