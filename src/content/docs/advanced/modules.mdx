---
title: Modules and Imports
description: Module layout, import paths, and visibility
sidebar:
  order: 1
---

Each `.fer` file is a module. Import syntax:

```ferret
import "std/net/tcp"
```

Access imported symbols via `::`.

```ferret
println("ok")
```

Visibility uses naming:

- uppercase symbol: exported
- lowercase symbol: module-private
